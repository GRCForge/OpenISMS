/// <reference types="vite/client" />

// Von vite.config.ts per `define` eingesetzt: die Version aus der
// VERSION-Datei. Haengt an der URL der Sprachdateien, damit ein Update seine
// Texte auch wirklich zeigt (siehe src/i18n.ts).
declare const __APP_VERSION__: string;
